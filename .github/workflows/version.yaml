name: ci

on:
  push:
    paths:
      - '**/src/**'
      - '**/tests/**'
      - 'Cargo.toml'
      - 'Cargo.lock'

jobs:
  version:
    uses: credibil/pipeline/.github/workflows/version.yaml@main
    # with:
    #   toolchain: stable
    #   target: x86_64-unknown-linux-gnu
    #   features: "full"